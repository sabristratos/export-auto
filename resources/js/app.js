import './bootstrap';

// Initialize Keys UI functionality
document.addEventListener('DOMContentLoaded', function() {
    // Keys UI auto-initializes when the <x-keys::scripts /> component is included
    // This ensures proper loading order with Livewire
    console.log('Keys UI and Livewire integration ready');
});
